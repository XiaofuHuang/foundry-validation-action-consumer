# Local custom-rule scenario

This hosted-agent sample has not been provisioned, deployed, run, or invoked.

The trusted workflow passes `custom-rules.yaml` through the Action's local
`rules-file` input. Its `DOC-001` rule is merged with the default rules.
